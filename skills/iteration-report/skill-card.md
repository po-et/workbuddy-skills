# 迭代周报生成器

把一个迭代周期内散落在 Git 与工单系统里的记录，重组成可验收、可追溯、可对比的交付报告。

- **报价值不报流水**：按工单/特性聚类，不是把 commit 列一遍
- **每条可溯源**：结论后挂 commit 短 hash 与工单链接
- **提交信息烂也能用**：自动识别空白/通用提交信息，从 diff 反推并标注「推断」
- **零鉴权起步**：Git 侧走本地 `git log`，不需要任何 token，内网 GitLab 同样可用
- **诚实**：数据缺口单列一节，不脑补

需要 `git` 与 `python3`。
