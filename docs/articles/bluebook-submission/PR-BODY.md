## Case 基本信息

- Case 标题：上线前五分钟体检：把 Dockerfile / K8s / SQL / OpenAPI / .env 五项检查串成一条 CI 门禁
- 使用场景：后端/平台工程师在发版前需要排查一批「事后看全都能静态查出来」的风险（镜像里的明文密码、没有 WHERE 的迁移语句、悄悄删掉的接口、生产少配的环境变量），但这些检查分属五个工具、五个人的习惯，平时没人会依次跑五遍
- 目标读者：负责发布 CI 门禁、上线值班或平台工程的同学；任何想把「发版前检查」从人工 checklist 变成一条命令 + 一个退出码的团队
- 使用的 Skills：`release-readiness-check`（自建开源，MIT，尚未上架 WorkBuddy/SkillHub 应用市场，需从仓库复制安装，见正文「使用的 Skill」一节）
- 是否需要外部账号或 API：否，全流程本地完成，不联网
- 是否涉及文件修改或对外发布：仅读写本地目标目录；CI 步骤在 workflow 内执行脚本，不访问外部服务

## 为什么值得收录

- 完整的真实收敛过程：第一次跑出 13 个 high（镜像明文密码、无 WHERE 的 UPDATE、NOT NULL 无 DEFAULT 的加列、被删的接口、生产少配的变量），按报告的「→ 改法」逐条处理到 0，门禁结论从「不建议上线」变成「可以上线，但先看 warn」，过程中的 SQL 扩展-收缩拆分、OpenAPI 兼容性改法都给了可抄的具体做法。
- 一段「修 warn 修出一个新 high」的真实插曲，说明为什么该按工具语义补配置、而不是改示例文件绕过检查。
- 三处边界如实写明：一次误报（占位符 DSN 被判成密钥）、一次漏报（`JWT_SIGNING_KEY` 与响应枚举收窄都没报出来）、一个 CI 假绿灯（空 JSON 当 OpenAPI base 会让所有接口误判为「新增」，必须改用 `--skip openapi`），不是只展示工具好用的一面。
- 可直接抄的 GitHub Actions 片段，两个分支（能取到旧 OpenAPI 规范 / 取不到）都在本机 bash 下实跑验证过。
- 已搜索现有 7 个社区案例目录名和标题、以及蓝皮书目录，未见同主题（发布前静态检查 / CI 门禁）的案例；蓝皮书第三篇《第 25 章 自动化工作流的可靠性》里的「上线前演练」是给*已上线的自动化定时任务*做场景演练，和本案例检查的对象（Dockerfile/K8s/SQL/OpenAPI/.env 五类发布风险）不重叠。

## 完整性检查

- [x] 已搜索社区案例集和蓝皮书，确认场景或任务没有重复（见上条搜索说明）
- [x] 已在 PR 中说明与蓝皮书第 25 章「上线前演练」的差异
- [x] 已描述真实使用场景
- [x] 已列出使用的 Skills 及其用途
- [x] 已写明可复现的操作步骤
- [ ] 已展示 WorkBuddy 中的实际效果 —— **如实说明**：本案例目前只有文本化的终端回显、报告摘录和前后对照表格（均为真实实跑结果），**没有 WorkBuddy 对话界面的图形截图**。案例集里已合并/在审的其他 Case 都至少配了 1 张 WorkBuddy 截图；PR #14（`meeting-audio-diarization`）就是因为缺截图，被维护者 liucongg 留言「是否可以补充在 workbuddy 里的截图」后挂起至今。是否要在提交前补一张终端截图，需要你决定，见同目录 README.md 的说明
- [x] 已提供明确的验收标准
- [x] 已说明失败情况、限制或安全边界
- [x] 已删除密钥、个人信息和敏感数据（全文域名统一 `example.com`，演示项目 `orders-api` 是为本文构造的最小服务，不涉及真实业务系统）
- [ ] 图片和其他素材拥有公开使用权限 —— 不适用，本案例暂无图片素材
- [ ] 已运行 `npm run docs:build` —— **尚未执行**：需要先 fork + clone 到本地仓库后才能跑（见 README.md 命令序列第 4 步），提交前必须跑一次确认构建通过
- [x] 本 PR 只提交一个 Case

## 结果证明

本案例没有 `assets/` 截图。「结果证明」以正文内嵌的真实材料呈现：

- 「在 WorkBuddy 中的效果」一节的两段真实终端回显（第一次体检 13 个 high / 收敛后终态 0 个 high），含 2026-09-18 本机实测的三次运行耗时（0.19s / 0.16s / 0.15s）
- 13 个 high 的完整摘录（规则号 + 位置 + 改法），和收敛前后的对照表格（high/warn/info/门禁结论/`--strict` 退出码 四项指标 × 三个时间点）
- 「遇到的问题」一节的两个真实踩坑记录（改配置引入新 high；CI 兜底逻辑制造假绿灯），均可按文中步骤复现

## 编辑授权

- [x] 同意维护者在不改变事实的前提下调整标题、结构和文字
- [x] 同意案例在保留署名的前提下被进一步整理进蓝皮书正文

---

格式方面如果和案例集的约定有出入，请直接在 PR 里说，我按要求改。

🤖 Generated with [Claude Code](https://claude.com/claude-code)
